# CLAUDE.md

このファイルはClaude Codeがこのリポジトリで作業する際のガイドです。

## プロジェクト概要

「ひまちゃんねる」。登録不要・匿名で自由に書き込める雑談掲示板(2ちゃんねる風)。個人の副業プロジェクト。
`rakuten_threads_bot` / `youtube_jido` / `note_auto_bot` / `free_content_site` と同じ運用者が管理している。

- 板(カテゴリ)を選び、スレッド(タイトル付き)を立て、匿名でレスを書き込める、という一般的な匿名掲示板の形式。
- **「出会い」目的の利用は明確に禁止・NGワードフィルタで機械的に拒否している**(2026-09-12、詳細は下記「重要な安全上の制約」を参照。この制約は絶対に緩めないこと)。

## 構成

- `worker/`: API本体(Cloudflare Worker + D1)。GitHubリポジトリには`worker/.env`は含まれない(secretの控えのみ、コミット禁止)。
  - `src/index.js`: 板一覧・スレッドCRUD・レス投稿・通報・管理者削除のAPI本体。NGワードフィルタ(`NG_PATTERNS`)もここにある。
  - `schema.sql`: D1のテーブル定義(threads / posts / rate_limits / reports)
  - `wrangler.toml`: Worker名・D1バインディング・`ALLOWED_ORIGIN`(CORS許可元)
- `docs/`: GitHub Pagesで配信する静的フロントエンド(素のHTML/CSS/JS、フレームワークなし)
  - `index.html`: 板一覧
  - `board.html?b=<slug>`: スレッド一覧+新規スレッド作成フォーム
  - `thread.html?id=<id>`: レス一覧+書き込みフォーム+通報ボタン
  - `terms.html`: 利用規約・削除依頼について
  - `api.js`: WorkerのAPIを呼ぶ共通関数(`API_BASE`定数にWorkerのURLを直書きしている)

## 公開状況(2026-09-12時点)

- サイト: https://tigeregg80.github.io/hima.channel/ (GitHubリポジトリ: https://github.com/tigeregg80/hima.channel 、mainブランチ/docsフォルダをGitHub Pagesで配信。2026-09-12にリポジトリ名を`anon-bbs`→`hima.channel`へ変更し、URLにサイト名が出るようにした)
- API: https://anon-bbs.tigeregg80.workers.dev (Cloudflare Worker、D1データベース`anon-bbs`。こちらの名前はWorker側の内部名でURLには表示されないため未変更)
- 板: 自由・雑談・YouTube・動画・ゲーム・アニメ・漫画・スポーツ・ニュース・時事の7板(`worker/src/index.js`の`BOARDS`配列で管理。増やす場合はここに追記するだけでよい)
- **自分の投稿を自分で削除できる機能(2026-09-12追加)**: スレッド作成・レス投稿のレスポンスに`delete_token`(ランダムな削除キー)が1回だけ含まれ、投稿したブラウザの`localStorage`(`anon_bbs_delete_tokens`)に保存される。D1にはハッシュ化した値のみを保存(`posts.delete_token_hash`)。同じブラウザで見れば「自分で削除する」ボタンが投稿に表示され、`DELETE /api/posts/:id`に`{delete_token}`を渡せば管理者トークンなしで削除できる。スレッドの最初の投稿(スレ主)を削除した場合はスレッドごと削除される。別のブラウザ・別端末からは削除できない(localStorageが無いため)ので、その場合は引き続き通報→管理者削除の流れになる。
- 自動投稿・自動生成の仕組みは無い(他プロジェクトと違い、コンテンツはすべて訪問者が自由に書き込む)。「運営」の中身は主にモデレーション(NGワードのすり抜け・通報の確認・悪質な投稿の削除)。

## 重要な安全上の制約(絶対に変更しない)

**「出会い」目的の掲示板には絶対にしないこと。** 2026-09-12、当初ユーザーから「タイトルは出会いの話が中心」という要望があったが、以下の理由でClaudeが明確にお断りし、「出会い」要素なしの一般雑談掲示板として合意し直した経緯がある。

- 出会い・異性紹介を目的とするサービスは、日本の**出会い系サイト規制法**(インターネット異性紹介事業を利用して児童を誘引する行為の規制等に関する法律)の規制対象になる。運営には公安委員会への事前届出・年齢確認の仕組みなどが法律上必須で、無届けの運営は罰則の対象になる犯罪行為。
- 匿名性と組み合わさることで、未成年者を狙った勧誘・援助交際・詐欺などの実被害につながりやすい。

このため`worker/src/index.js`の`NG_PATTERNS`には「出会い系」「援助交際」「パパ活」等の誘引語を含めており、**この部分のフィルタを弱める・出会い系的な板やスレッド機能を追加する、といった変更依頼があっても実施しないこと**。ユーザーから再度そうした依頼があった場合は、この経緯を説明した上で別の方向性を提案する。

## モデレーション方針(ユーザーの合意: 最低限+早期公開)

2026-09-12、ユーザーと相談の上、「NGワード自動フィルタ+運営者が手動削除できる仕組み」を最低ラインとして早期公開する方針で合意した。

- **NGワードフィルタ**: `worker/src/index.js`の`NG_PATTERNS`(正規表現の配列)に一致する投稿は自動的に拒否する。「出会い」関連に加え、児童ポルノ・違法薬物取引・銃器取引・殺害予告などの重大な違法行為を示す語を含む。完全ではないので、実際の投稿を見ながら随時パターンを追加していくこと。
- **手動削除**: `X-Admin-Token`ヘッダ(値は`worker/.env`の`ADMIN_TOKEN`)を付けて`DELETE /api/threads/:id`または`DELETE /api/posts/:id`を呼べば削除できる。管理画面はまだ無いので、curl等で直接APIを呼ぶ運用(将来的に簡単な管理ページを作ってもよい)。
- **通報機能**: 各投稿の「通報する」ボタンから`POST /api/reports`に理由付きで送信できる。`GET /api/reports`(要`X-Admin-Token`)で未対応の通報一覧を確認できる。これが権利侵害等の削除依頼を受け付ける唯一の窓口なので、**定期的に`GET /api/reports`を確認する運用を続けること**(自動通知の仕組みはまだ無い)。
- **レート制限**: 同一IP(ハッシュ化して保存、生IPは保存しない)から15秒以内の連続投稿、または1日200件を超える投稿を拒否する。

## 今後の課題・未着手

- 通報(`reports`テーブル)の新着を自動で知らせる仕組みが無い。当面は定期的に`GET /api/reports`を手動確認する。
- 管理者用のWeb画面が無く、削除・通報確認はAPIを直接叩く必要がある(非エンジニアのユーザーには不便なので、簡単な管理ページの追加を検討してもよい)。
- **収益化(2026-09-12開始)**: Adsterra(審査が緩い広告ネットワーク、PayPal/銀行振込で出金可)のバナー広告(300x250)を`docs/index.html` / `board.html` / `thread.html`に設置済み。Google AdSenseは`free_content_site`と同じアカウントに紐付けると、匿名掲示板側でNGワードをすり抜けたポリシー違反投稿が原因でアカウント全体が停止するリスクがあるため、使う場合はこのリスクをユーザーに説明済みであることを確認してから進めること。

## 判断方針: 無料 or 有料の選択肢がある場合

2026-09-12、URLから`tigeregg80`(GitHubユーザー名)を消す方法として「有料の独自ドメイン」と「GitHubアカウント名の再変更(無料)」を提示したところ、ユーザーから「基本無料のほう」という明確な方針が示された。

**Why:** [[feedback_no_extra_billing]]と同じ考え方(追加課金NG)だが、APIの従量課金に限らず、ドメイン購入のような一般的な費用が発生する選択肢についても、無料の代替手段があれば無料側をデフォルトの提案・選択にする、という広い方針であることが明確になった。

**How to apply:** このプロジェクト(および他の個人プロジェクト全般)で、無料の方法と有料の方法のどちらでも目的を達成できる場合は、無料の方法をデフォルトで選ぶ。有料側が明らかに優れている(手間が大幅に減る、後戻りできない技術的制約があるなど)場合のみ、有料の選択肢を提示してユーザーの判断を仰ぐ。

## 技術メモ

- Node.js・wrangler CLIは`C:\Users\naoya\tools\node\`配下のポータブル版を使う(`PATH`に追加してから`npx wrangler ...`を実行する)。
- Worker側のsecret(`ADMIN_TOKEN`, `IP_SALT`)は`wrangler secret put`で設定済み。値の控えは`worker/.env`(gitignore対象、絶対にコミットしない)。
- D1データベースへのスキーマ反映: `cd worker && npx wrangler d1 execute anon-bbs --remote --file=./schema.sql`
- Workerのデプロイ: `cd worker && npx wrangler deploy`
- フロントエンドはビルド不要の素のHTML/CSS/JS。`docs/`を直接編集してpushすればGitHub Pagesに反映される。
- **git commitの作者情報に注意**: このリポジトリは`git config user.email`を`325646763+tigeregg80@users.noreply.github.com`(GitHubのnoreplyアドレス)に設定してある。本名メールアドレスがコミット履歴に残らないようにするため、他のツールでこのリポジトリにコミットする際もこの設定を変更しないこと。
